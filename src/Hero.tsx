import React from 'react';
import './Hero.css';

const Hero: React.FC = () => (
  <section className="hero">
    <h1 className="hero-title">Abundance</h1>
    <p className="hero-subtitle">
      Education seeds abundance. True peace and prosperity begin when we invest in our children.
    </p>
  </section>
);

export default Hero; 